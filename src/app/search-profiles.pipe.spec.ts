import { SearchProfilesPipe } from './search-profiles.pipe';

describe('SearchProfilesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProfilesPipe();
    expect(pipe).toBeTruthy();
  });
});
